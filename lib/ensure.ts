// thin re-export — single truth lives in lib/db.ts
export {
  ensureUsers,
  ensureEvents,
  ensureVerifications,
  ensureMiningLogs,
  ensureCanonicalLog,
  ensureScopeVotes,
  ensureScopeResolution,
  ensureAllTables,
  ensureUsersTable,
  ensureEventsTable,
  ensureVerificationsTable,
  ensureMiningLogsTable,
  ensureMiningTable,
  ensureCanonicalLogTable,
  ensureScopeVotes as ensureScopeVotesTable,
  ensureScopeResolution as ensureScopeResolutionTable,
  ensureTables,
} from "./db";
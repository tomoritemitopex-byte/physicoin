DATABASE_URL=postgresql://USER:PASSWORD@ep-xxx.neon.tech/neondb?sslmode=require
# Optional: DATABASE_URLS=comma-separated list for sharding (read lib/data.ts)
# Required in Vercel: CRON_SECRET (signs weekly faucet drip) → Settings → Environment Variables
# Secret-only (never commit): BOT_TOKEN, TELEGRAM_*, WHATSAPP_*

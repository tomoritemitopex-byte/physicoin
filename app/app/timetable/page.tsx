export default function TimetablePage() {
  return <div className="rounded-2xl border border-white/[0.06] bg-white/[0.02] p-6 text-slate-300">Timetable — live feed ships Phase 3. API: <code className="rounded bg-white/10 px-1">/api/timetable</code></div>;
}
